# PostgreSQL connection string (used by the app and Drizzle).
# Example for local Postgres: postgres://user:password@localhost:5432/agency_docs
# For Neon, use the connection string from your project dashboard.
DATABASE_URL=
